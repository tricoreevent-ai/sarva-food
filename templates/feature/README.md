# Feature Template

Use this folder as the copy source for new bounded features.

Replace:

- `__FEATURE_NAME__` with a PascalCase feature name.
- `__FEATURE_TITLE__` with the human-facing screen title.

Keep new features mock-data driven first, then wire Firebase through `src/services` with pagination, shared listeners, and route-level loading.
