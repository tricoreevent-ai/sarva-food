# Restaurant Health Dashboard Tests

Run:

```bash
npm run test:enhancements
npm run typecheck
npm run lint
```

The enhancement audit validates metadata, SDK-only imports, all extension points, runtime lifecycle coverage, storage modes, routes, permissions, feature flag defaults, memory cleanup, performance, and regression boundaries.
