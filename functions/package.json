{
  "name": "sarva-food-functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "13.9.0",
    "firebase-functions": "7.2.5"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
