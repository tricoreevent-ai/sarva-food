# Plugin Context

Every plugin receives an isolated `PluginContext` containing plugin id, version, runtime version, permissions, logger, event bus, config, storage, navigation, router, theme, environment, user, tenant, language, timezone, diagnostics, approved API, UI, assets, and services.

Internal application services are not exposed.
