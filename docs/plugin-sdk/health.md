# Plugin Health

The health monitor tracks heartbeat, runtime errors, memory bytes, FPS, long tasks, leaks, failures, health score, and automatic disable state.

Plugins are disabled after repeated runtime failures.
