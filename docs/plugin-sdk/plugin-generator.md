# Plugin Generator

Command:

```bash
npm run plugin:create
```

The generator creates metadata, config, feature flag file, tests, docs, validator, routes, UI, hooks, services, types, and README scaffolding.
