# Enterprise Performance Recovery Report

Date: 2026-07-08; RC5 addendum 2026-07-13
Branch: `release/production-nammude`
Current RC5 base HEAD: `b8c1ed6a7d4310f80cd9fdbe9b8621e21d5fc132` plus synchronized workspace
Runtime tag: recommended `v1.0.0-rc5`; existing `v1.0.0-rc4` remains unchanged

## Measurement Status

| Source | Result |
| --- | --- |
| Hosted `/api/release-info` | Current hosted checks still report `deploymentEnvironment: development`; Hostinger must serve RC5 with `NEXT_PUBLIC_APP_ENV=production` and `NEXT_PUBLIC_APP_VERSION=v1.0.0-rc5`. |
| PageSpeed Insights API | Blocked by Google API quota `429 RESOURCE_EXHAUSTED`; current external Lighthouse could not be rerun from this workspace. |
| Saved Lighthouse artifacts | `tmp/lighthouse-mobile.json` and `tmp/lighthouse-desktop.json` are from 2026-07-06 stale deployment and used only for root-cause direction. |
| Local bundle analyzer | `.next/analyze/client.html`, `.next/analyze/nodejs.html`, `.next/analyze/edge.html` generated by `npm run analyze`. |
| Current build assets | `.next/static/chunks` and `.next/static/css` inspected after the latest build/analyze run. |

## Root Cause Summary

| Priority | Bottleneck | Evidence | Impact |
| --- | --- | --- | --- |
| P0 | Home LCP is text render delay, not image load. | Saved Lighthouse LCP element is `main.min-h-screen > section.container-page > div.grid > p.text-sm`, the "Nammude is a direct-to-customer..." paragraph; 93% of LCP was render delay. | JS/hydration and late content replacement delay the visible home content. |
| P0 | Large CLS comes from page/footer movement after client data resolves. | Saved Lighthouse found CLS `0.863`; primary shifted node is the customer shell/footer area. | Skeleton/content height mismatch moves lower content significantly on mobile. |
| P0 | Customer shell starts action-only/auth-heavy work too early. | `CustomerShellRuntime` mounts `AuthSessionBridge`, `FirestoreStoreHydrator`, `AppToaster`, PWA, push, analytics, and full `CustomerShellClient`; `CustomerShellClient` imports logout services statically. | More JS and Firebase/auth modules execute before first paint. |
| P0 | Public header pulls Firebase/Firestore into initial customer bundle. | `PublicHeader` statically imports `firebase/firestore` and `@/firebase/client` for saved addresses even though addresses are only needed when the location picker opens. | Firestore/auth SDK cost appears on public home. |
| P0 | Home imports Firebase write helpers for favorite action before they are used. | `CustomerDiscoveryHome` statically imports customer favorite save/delete services. | Action-only Firestore code inflates and executes in the home route. |
| P0 | Firebase client has eager compatibility exports. | `src/firebase/client.ts` exports `app`, `auth`, `db`, `storage`, and `analytics` by calling resolver functions at module evaluation. | Any import from the module can initialize Firebase subsystems earlier than necessary. |
| P0 | Google Analytics loads too early. | Saved Lighthouse third-party summary shows Google Tag Manager blocking main thread for about `450ms`; root layout uses `afterInteractive`. | Increases TBT/boot cost during first interaction window. |
| P1 | Home route starts multiple client fetches before idle. | Saved Lighthouse network shows `/api/auth/session`, `/api/public/restaurants`, `/api/public/cms`, `/api/user/preferences`, `/api/public/categories`, `/api/public/menu`, `/api/public/offers`, Firebase config/installations, and many RSC prefetches. | Increases CPU/network contention before LCP. |
| P1 | Shared customer chunks remain heavy. | Current `.next/static/chunks`: `3143` 788 KB, `3112` 589 KB, `02df5fe5` 244 KB, CSS 183 KB. | Unused JS and parse/compile cost still constrain mobile score. |

## LCP Investigation

- Stale mobile LCP: `12.8s`.
- LCP element: the home explanatory paragraph below the hero.
- LCP phases: TTFB `854ms`, render delay about `11.99s` in the saved full audit and `5.68s` in the insight table.
- Conclusion: the LCP target is delayed by client-rendered route content and main-thread work, not by an oversized LCP image.
- Immediate fix direction: reduce initial JS/third-party execution and keep the home skeleton/content height stable so text can paint without large reflow.

## CLS Investigation

- Stale mobile CLS: `0.863`.
- Lighthouse layout-shift audit: 2 shifts; dominant shifted node is the shell/footer block.
- Likely current cause: home skeleton and loaded home content differ greatly in height; dynamic customer shell/footer loads after the skeleton.
- Immediate fix direction: reserve mobile and desktop home route height and move non-critical shell/runtime work after idle.

## Bundle And Dependency Audit

Current largest emitted chunks after local analyze:

| Asset | Size | Finding |
| --- | ---: | --- |
| `c36f3faa...js` | 1.75 MB | Mapbox runtime; must stay off non-map routes. |
| `3143...js` | 788 KB | Large shared/app chunk; inspect after code changes. |
| `3112...js` | 589 KB | Account/auth/profile shared chunk; saved Lighthouse flagged high unused JS. |
| `2170a4aa...js` | 412 KB | Owner/Admin menu-library import/export family. |
| `02df5fe5...js` | 266 KB | Firestore common chunk; Phase 2 reduced route ownership from 94 manifests to 10 and removed it from sampled `/` and `/profile` initial HTML. |
| `b99dc2eda650e132.css` | 183 KB | Main CSS; visual CSS cleanup requires browser regression pass. |

Largest dependency risks: Firebase/Auth/Firestore, Stack auth, Mapbox, Framer Motion, Lucide, React Hot Toast, Xlsx, Google Analytics.

## Provider Report

| Provider | Current behavior | Action |
| --- | --- | --- |
| Root theme/i18n | Required globally. | Keep. |
| Alert/toast | Alert provider is global in customer shell; toaster mounted immediately. | Defer toaster until idle/action. |
| Auth session bridge | Mounted immediately in customer shell. | Defer on public routes; preserve auth logic. |
| Firestore store hydrator | Mounted immediately in customer shell. | Defer until idle. |
| PWA/push/analytics provider | Already idle-mounted. | Keep. |
| Public header address listener | Starts only when logged in, but code is in initial bundle. | Dynamic import and run only when picker opens. |

## Firestore And Network Report

- Public restaurant/category/menu/offers use REST API fetches with in-flight dedupe and memory cache.
- No public homepage direct realtime listener was confirmed except the header customer-address listener path for logged-in users.
- Saved Lighthouse shows Firebase auth/config/installations requests on the home route; these should be deferred away from first paint.
- Menu/offers for the first restaurant are not needed for the initial hero and should not block LCP.

## Hydration Report

- Root layout is server-rendered and currently minimal.
- Customer public routes hydrate `CustomerShellClient`, `PublicHeader`, `CustomerAuthProvider`, cart sync, offline banner, footer, mobile nav, and the route flow.
- Home flow is a large client component and owns many hooks before the first visible stable paint.
- Immediate safe work: move action-only modules to dynamic imports and idle-mount non-critical runtime providers. Full server-component conversion needs a separate data architecture pass.

## Implementation Plan

Completed in this sprint:

| Root cause | Files changed | Runtime fix | Expected impact |
| --- | --- | --- | --- |
| Google Analytics main-thread work during the interactive window. | `src/app/layout.tsx` | Changed GA scripts from `afterInteractive` to `lazyOnload`. | Reduces third-party competition before LCP/early input. |
| Customer shell mounted non-critical runtime providers immediately. | `src/components/layout/customer-shell-runtime.tsx` | Moved auth session bridge, Firestore hydrator, toaster, PWA, push, and analytics diagnostics behind `IdleMount`. | Less hydration and provider work before first paint. |
| Logout-only auth modules were in the customer shell initial bundle. | `src/components/layout/customer-shell-client.tsx`, `src/components/layout/public-header.tsx` | Moved sign-out service imports into logout handlers. | Keeps action-only auth modules off the startup path. |
| Header imported Firestore for saved addresses before the location picker was opened. | `src/components/layout/public-header.tsx` | Dynamic-imports Firestore/client/collections only while the location picker is open and a customer is signed in. | Removes a public-route Firestore listener/import from first paint. |
| Home favorite write helpers loaded before a favorite click. | `src/components/flows/customer-discovery-home.tsx` | Dynamic-imports favorite save/delete helpers inside the favorite action. | Removes action-only Firestore write helpers from initial home execution. |
| Home fetched hero-restaurant menu data before it was needed below the fold. | `src/components/flows/customer-discovery-home.tsx` | Defers menu preview fetch until browser idle. | Reduces early API/cache work and first-paint contention. |
| Firebase compatibility exports initialized app/auth/db/storage/analytics on module import. | `src/firebase/client.ts` | Replaced eager compatibility exports with non-eager null exports; preserved accessor functions. Emulator wiring now evaluates storage/db only when emulators are enabled. | Prevents accidental Firebase subsystem startup from shared imports. |
| Home loading fallback did not reserve final page height. | `src/components/state/page-state.tsx` | Replaced the fixed full-screen splash with a layout-matching skeleton and reserved mobile/desktop content height. | Reduces footer/page movement contributing to CLS. |

No business logic, API contract, Firestore collection, schema, auth flow, Owner, Kitchen, POS, Admin, or repository behavior was intentionally changed.

## Validation Results

| Check | Result |
| --- | --- |
| `cmd /c npm run typecheck` | Passed |
| `cmd /c npm run lint` | Passed |
| `cmd /c npm run build` | Passed after network approval for `next/font`; retains known Firebase/protobuf dynamic dependency warning. |
| `cmd /c npm run analyze` | Passed; regenerated `.next/analyze/client.html`, `.next/analyze/nodejs.html`, and `.next/analyze/edge.html`. |
| `cmd /c npm run audit:release` | Passed; `0` debt markers and `0` matching unbounded Firestore collection reads. |
| `cmd /c npm run smoke:operational` | Passed |
| `git diff --check` | Passed with Git line-ending normalization warnings only. |

## Final Bundle Snapshot

Largest current emitted client chunks after `cmd /c npm run analyze`:

| Asset | Size | Finding |
| --- | ---: | --- |
| `c36f3faa...js` | 1.75 MB | Mapbox runtime; still isolated to map-capable routes. |
| `3143...js` | 788 KB | Large shared/app chunk; next candidate for deeper route ownership audit. |
| `3112...js` | 589 KB | Account/auth/profile shared chunk; profile split remains the largest deferred opportunity. |
| `2170a4aa...js` | 412 KB | Owner/admin menu-library import/export family. |
| `02df5fe5...js` | 266 KB | Broad customer/public shared chunk; startup imports were reduced but full server-component conversion remains future work. |
| `33006b0b0519f4d7.css` | 183 KB | Main CSS; cleanup requires browser visual regression coverage. |

## Phase 2 Route-Level Splitting

Completed after the initial performance recovery pass:

| Area | Result |
| --- | --- |
| Reports | Added `docs/performance/BUNDLE_DEEP_ANALYSIS.md`, `docs/performance/DEPENDENCY_AUDIT.md`, `docs/performance/ROUTE_LOAD_ANALYSIS.md`, and `docs/performance/PERFORMANCE_PHASE2_REPORT.md`. |
| Firebase/Auth ownership | Added config-only Firebase module and moved auth/profile/action SDK imports to effect/action time. |
| App store ownership | Moved owner/menu/staff/inventory Firestore mutation services out of the public shell startup graph and into dynamic action imports. |
| `/` route JS | Reduced from `1017 KB` to `455 KB`. |
| `/profile` route JS | Reduced from `1714 KB` to `562 KB`. |
| Firestore/Auth ownership | Reduced from `94` route manifests to `10`. |
| Initial chunk probe | `/`, `/profile`, and `/owner/pos` do not initially load Firestore/Auth/Stack/XLSX/Mapbox flagged chunks; `/login` keeps auth chunks as expected. |

## Validation Plan

Completed after implementation:

```bat
cmd /c npm run typecheck
cmd /c npm run lint
cmd /c npm run build
cmd /c npm run analyze
cmd /c npm run audit:release
cmd /c npm run smoke:operational
git diff --check
```

Current external Lighthouse cannot be rerun here because PageSpeed API quota is exhausted and no local Lighthouse CLI/Chrome executable is available in this workspace. Production Lighthouse must be rerun after Hostinger serves production env.

## Remaining Runtime Opportunities

| Priority | Work | Why not completed in this pass |
| --- | --- | --- |
| P0 | Rerun Lighthouse/Chrome Performance/Coverage/Memory against the corrected production deployment. | PageSpeed API returned quota `429`; local Lighthouse/Chrome executable was unavailable. |
| P1 | Split `/profile` by existing tabs/actions. | Larger code-splitting pass needs authenticated browser smoke. |
| P1 | Convert more public home data to server-rendered/cache-backed islands. | Requires a separate data architecture pass to avoid changing public API/cache contracts. |
| P1 | Continue splitting owner orders/settings and motion-heavy route ownership. | Needs route-by-route authenticated browser validation to avoid operational regressions. |

## RC5 Pending Work Audit Addendum - 2026-07-13

| Area | Result |
| --- | --- |
| Bundle evidence | `docs/performance/FINAL_BUNDLE_REPORT.md` and `docs/performance/BUNDLE_DEEP_ANALYSIS.md` regenerated from the current analyzer output. Static JS is `8776 KB`, CSS is `190 KB`, and `/owner/orders` is `1246 KB` against the `1200 KB` warning threshold. |
| Source scan | No actionable runtime TODO/FIXME/HACK/XXX, app-source `console.log`, stale duplicate order component, or unbounded Firestore collection read was found. |
| Listener/memory scan | Kitchen SSE, POS listeners, Active Orders portaled action menu, order polling, and offline sync singleton were reviewed; cleanup or singleton scope is present. |
| Final optimization cleanup | Duplicated client error-reason helpers now use `src/lib/client-diagnostics.ts`; compact order action controls now include explicit accessible names. |
| Decision | No additional freeze-safe repository performance change was applied. Deeper owner/profile/motion splits require authenticated browser smoke, Chrome Performance/Coverage, and Lighthouse after Hostinger env correction. |
