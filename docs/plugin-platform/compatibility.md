# Compatibility

Compatibility checks cover application version, plugin runtime version, platform, environment, supported modules, browser hints, feature flags, permissions, and plugin API version readiness.
