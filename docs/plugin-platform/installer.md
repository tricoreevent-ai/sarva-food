# Plugin Installer

Install flow:

```text
Download -> Validate -> Resolve Dependencies -> Compatibility -> Registry -> Initialize -> Enable -> Health Check -> Complete
```

Any failed step rolls back the entire transaction.
