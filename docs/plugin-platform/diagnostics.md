# Diagnostics

Diagnostics report installed, registered, enabled, disabled, broken plugins, memory signal, load time, initialization time, validation failures, dependency failures, runtime failures, health score, average startup, bundle size, and install size.
