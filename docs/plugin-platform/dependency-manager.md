# Dependency Manager

The dependency manager validates hard, peer, optional, soft, and development dependencies. It detects missing dependencies, disabled dependencies, version mismatches, circular references, and broken graphs.

Outputs include dependency tree, graph, validation issues, and topological execution order.
