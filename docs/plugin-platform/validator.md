# Validator

Validation pipeline:

```text
Metadata -> Dependencies -> Compatibility -> Permissions -> Config Schema -> Lifecycle -> Health -> Registration -> Enable
```

Reports include passed state, warnings, errors, blocking issues, and recovery suggestions.
